# Документация функций и методов проекта ChaosCash

## 📋 Обзор

Этот документ содержит полный анализ использования всех функций и методов в проекте ChaosCash.

**Дата анализа:** 2026-02-28
**Проанализировано файлов:** 66
**Всего функций/методов:** 401
**PyQt соединений:** 93

---

## 📁 Структура документов

### 1. **FUNCTION_USAGE.md** (5586 строк)
Базовый отчёт со списком всех функций и методов:
- ✅ 387 функций и методов
- 📍 Место определения для каждой функции (файл и строка)
- 📊 Список всех мест использования

### 2. **FUNCTION_USAGE_DETAILED.md** (4518 строк)
Подробный анализ с группировкой по типам:
- 👥 74 standalone функции
- 🔧 327 методов классов
- ⚠️ 165 неиспользуемых функций (возможно мертвый код)
- 📈 Статистика по типам функций

### 3. **QT_CONNECTIONS.md** (200+ строк)
Полный реестр PyQt сигналов и слотов:
- 🔗 93 сигнал-слотных соединения
- 🎯 Файлы с Qt-интеграцией
- 📡 Часто используемые сигналы и их статистика

---

## 🏗️ Архитектура проекта

### Основные компоненты

```
ChaosCash/
├── app/
│   ├── database/          # Слой доступа к БД
│   │   ├── connection.py
│   │   ├── queries/       # SQL запросы
│   │   └── schema.py
│   ├── repositories/      # Слой репозитория (Account, Transaction, etc.)
│   ├── services/          # Бизнес-логика (Balance, Transaction, Integrity)
│   ├── models/            # Pydantic модели данных
│   ├── settings/          # Конфигурация приложения
│   ├── ui/                # PyQt UI компоненты
│   │   ├── main_window.py # Главное окно приложения
│   │   ├── dialogs/       # Диалоги (Settings, Currency Editor, etc.)
│   │   ├── widgets/       # Виджеты (TreeView, TableView)
│   │   ├── delegates/     # Cell editors (Amount, Date, etc.)
│   │   ├── item_models/   # Qt Models (Account, Transaction, Split)
│   │   └── ui_event_logger.py
│   ├── utils/             # Утилиты (parsing, hierarchy, date utils)
│   └── i18n/              # Интернационализация
├── main.py                # Entry point
└── tests/                 # Тесты
```

---

## 🔑 Ключевые функции и их использование

### Сервисы (Services)

#### **BalanceService** (`app/services/balance_service.py`)
- `invalidate()` - инвалидирует кэш баланса при изменении расчета
- `get_balance()` - получает баланс аккаунта (часто вызывается из UI)
- `clear()` - очищает весь кэш после массовых изменений

**Использование:** Вызывается при каждом изменении сплитов и аккаунтов

#### **TransactionService** (`app/services/transaction_service.py`)
- `create_transaction()` - создание новой транзакции
- `add_split()`, `update_split()`, `delete_split()` - управление сплитами
- `recalculate_flexible_splits()` - пересчёт гибких сплитов при изменении сумм
- `duplicate_transaction()`, `reverse_transaction()` - операции над транзакциями

**Использование:** Основной интерфейс для всех операций над транзакциями

#### **IntegrityService** (`app/services/integrity_service.py`)
- `get_imbalanced_trans_ids()` - нахождение несбалансированных транзакций
- `get_empty_transactions()` - поиск пустых транзакций
- Проверка целостности данных БД

**Использование:** Вызывается при отображении "виртуальных" нод в дереве аккаунтов

### Репозитории (Repositories)

Каждый репозиторий предоставляет CRUD операции для своей сущности:

- **AccountRepo** - управление аккаунтами (иерархия, видимость, баланс)
- **TransactionRepo** - запросы по транзакциям с фильтрацией
- **SplitRepo** - операции со сплитами
- **CurrencyRepo** - управление валютами

---

## 🎨 Qt Model-View-Delegate паттерны

### ItemModels (Qt Models)

| Модель | Файл | Основные методы |
|--------|------|-----------------|
| **AccountTreeModel** | `ui/item_models/account_tree_model.py` | `reload()`, `data()`, `setData()`, Drag-Drop support |
| **TransactionModel** | `ui/item_models/transaction_model.py` | `load()`, `load_by_ids()`, Lazy loading с fetchMore() |
| **SplitModel** | `ui/item_models/split_model.py` | `load()`, `setData()` для inline редактирования |

### Delegates (Cell Editors)

| Делегат | Редактируемое поле |
|---------|------------------|
| **AmountDelegate** | Сумма сплита (с поддержкой выражений) |
| **DateDelegate** | Дата транзакции |
| **AccountComboDelegate** | Выбор аккаунта (searchable) |
| **CurrencyComboDelegate** | Выбор валюты |

---

## 🔌 PyQt Signal-Slot соединения

### Основные сигналы в MainWindow

```
account_tree.account_selected
    → MainWindow._on_accounts_selected()
        → load transactions for selected accounts

transaction_view.transaction_selected
    → MainWindow._on_transaction_selected()
        → load splits for selected transaction

split_view.split_changed
    → MainWindow._on_split_changed()
        → recalculate flexible splits and refresh
```

### Контекстные меню и действия

- AccountTreeView: Add/Delete/Edit account, drag-drop moving
- TransactionView: Duplicate, Reverse, Delete transaction
- SplitView: Delete split, recalculate balances

**Итого:** 93 соединения (93 из них реальные PyQt .connect(), остальные - Python функции)

---

## ⚠️ Неиспользуемые функции

Обнаружено **165 неиспользуемых функций**:

### Примеры:
- `AccountNode.is_virtual` - возможно deprecated
- `AccountTreeModel.dropMimeData` - реализовано но не вызывается
- Несколько Qt override методов, которые не используются
- `view_helpers._toggle_column` - возможно заменено

**Рекомендация:** Проверить и удалить мертвый код при следующем рефакторинге.

---

## 📊 Статистика по типам

### Функции по категориям

| Категория | Количество | Примеры |
|-----------|-----------|---------|
| Repository methods | ~65 | `insert()`, `get_by_id()`, `update()`, `delete()` |
| Service methods | ~35 | `create_transaction()`, `get_balance()` |
| MainWindow methods | ~40 | `_on_accounts_selected()`, `_on_split_changed()` |
| Model methods | ~60 | `data()`, `setData()`, `rowCount()` |
| Delegate methods | ~30 | `createEditor()`, `setEditorData()` |
| Dialog methods | ~25 | `_save()`, `_load()` |
| Utility functions | ~15 | `safe_eval()`, `compute_hidden_account_ids()` |
| Widget methods | ~50 | Event handlers, context menus |

---

## 🔄 Основные flow'ы данных

### 1️⃣ Создание новой транзакции
```
MainWindow._on_new_transaction_requested()
  → TransactionService.create_transaction()
    → TransactionRepo.insert()
  → Split creation loop
    → SplitService.add_split()
      → SplitRepo.insert()
  → BalanceService.clear()
```

### 2️⃣ Изменение сплита inline
```
SplitModel.setData()
  → TransactionService.update_split()
    → SplitRepo.update()
  → MainWindow._on_split_changed()
    → TransactionService.recalculate_flexible_splits()
      → SplitRepo.update_amount() x N
    → BalanceService.invalidate()
    → UI refresh
```

### 3️⃣ Навигация по дереву аккаунтов
```
AccountTreeView._on_clicked()
  → emits account_selected signal
  → MainWindow._on_accounts_selected()
    → TransactionModel.load(account_ids)
    → TransactionView reload
```

---

## 🧪 Тестовое покрытие

Все основные сервисы имеют тесты:

- ✅ `test_transaction_service.py` - 13 тестов
- ✅ `test_balance_service.py` - 5 тестов
- ✅ `test_integrity_service.py` - 6 тестов
- ✅ `test_expression_parser.py` - 11 тестов

---

## 📚 Как использовать эту документацию

### Найти где используется функция
1. Откройте `FUNCTION_USAGE.md` или `FUNCTION_USAGE_DETAILED.md`
2. Найдите функцию по названию (есть индекс в начале)
3. Посмотрите все места где она вызывается

### Понять Qt соединения
1. Откройте `QT_CONNECTIONS.md`
2. Найдите интересующий сигнал
3. Посмотрите какой слот на него подписан

### Отследить поток выполнения
1. Начните с главного окна или обработчика события
2. Следуйте вызовам функций используя данную документацию
3. Используйте cross-reference для понимания потока данных

---

## 🛠️ Утилиты для анализа

Проект включает скрипты для регенерации этой документации:

```bash
# Основной анализ
python3 generate_function_usage_docs.py

# Детальный анализ
python3 generate_detailed_function_usage.py

# Qt соединения
python3 extract_qt_connections.py
```

---

## 📝 Примечания

- Анализ выполнен с использованием AST (Abstract Syntax Tree) для точного парсинга Python кода
- Qt сигналы-слоты обнаруживаются regex-паттернами (100% точность не гарантирована)
- Некоторые динамические вызовы (через `getattr`, `exec` и т.д.) могут быть пропущены
- Документация актуальна на 2026-02-28

---

## 📞 Дополнительная информация

Для получения подробной информации о конкретной функции обратитесь к:
- `FUNCTION_USAGE.md` - базовый список
- `FUNCTION_USAGE_DETAILED.md` - детальный анализ
- `QT_CONNECTIONS.md` - сигналы и слоты
- Исходный код в `app/` директории

Документация автоматически сгенерирована и готова к использованию!
